import { spicy } from 'candlelight/helpers/spicy';
import { module, test } from 'qunit';

module('Unit | Helper | spicy');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = spicy([42]);
  assert.ok(result);
});
